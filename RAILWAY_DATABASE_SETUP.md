# Настройка постоянной базы данных PostgreSQL в Railway

## Проблема
Если база данных пересоздается при каждом деплое, все данные (уроки, курсы) теряются.

## Решение: Настройка постоянной базы данных

### Шаг 1: Создание PostgreSQL сервиса в Railway

1. Откройте ваш проект в Railway: https://railway.app
2. В панели проекта нажмите **"+ New"** (или **"New"**)
3. Выберите **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст PostgreSQL базу данных

### Шаг 2: Подключение базы данных к приложению

1. После создания базы данных, Railway автоматически создаст переменную окружения `DATABASE_URL`
2. В панели проекта найдите ваш **Web Service** (ваше приложение)
3. Перейдите в **"Variables"** (Переменные окружения)
4. Убедитесь, что переменная `DATABASE_URL` существует и содержит строку подключения к PostgreSQL
   - Формат: `postgresql://user:password@host:port/database`
   - Railway автоматически подставляет правильные значения

### Шаг 3: Проверка настройки

1. В панели проекта найдите ваш **PostgreSQL сервис**
2. Убедитесь, что он имеет статус **"Active"** (Активен)
3. Проверьте, что база данных не помечена как временная (temporary)
4. В настройках базы данных должно быть указано, что это **persistent storage** (постоянное хранилище)

### Шаг 4: Проверка после деплоя

1. После деплоя проверьте логи приложения в Railway
2. Найдите строки:
   - `📊 Текущее количество записей в courses_data: X` - должно показывать количество записей
   - Если видите `📦 База данных пуста, миграция данных...` - значит база была пересоздана
3. Если база пересоздается каждый раз:
   - Проверьте, что `DATABASE_URL` указывает на правильную базу
   - Убедитесь, что база данных не удаляется при перезапуске сервиса

### Важные моменты

- **Постоянная база данных** сохраняет данные между деплоями
- **Временная база данных** удаляется при перезапуске/деплое
- Railway по умолчанию создает постоянные базы данных, но проверьте настройки
- Если вы используете бесплатный план Railway, база данных может иметь ограничения

### Альтернативное решение

Если проблема сохраняется, можно использовать внешнюю базу данных:
- **Supabase** (бесплатный план доступен)
- **Neon** (бесплатный план доступен)
- **Render PostgreSQL** (бесплатный план доступен)

Просто создайте базу данных в одном из этих сервисов и укажите `DATABASE_URL` в Railway.

