# Настройка постоянной базы данных PostgreSQL в Railway

## Проблема
Если база данных пересоздается при каждом деплое, все данные (уроки, курсы) теряются.

## Решение: Настройка постоянной базы данных

### Шаг 1: Создание PostgreSQL сервиса в Railway

1. Откройте ваш проект в Railway: https://railway.app
2. В панели проекта нажмите **"+ New"** (или **"New"**)
3. Выберите **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст PostgreSQL базу данных

### Шаг 2: Подключение базы данных к приложению (ВАЖНО!)

**Проблема:** База данных создана, но переменные окружения не подключены к вашему приложению.

**Решение - Добавить Variable Reference:**

1. В панели проекта найдите ваш **Web Service** (ваше приложение) - это сервис, который запускает ваше Next.js приложение
2. Откройте ваш **Web Service** и перейдите в раздел **"Variables"** (Переменные окружения)
3. Нажмите кнопку **"+ New Variable"** или **"Add Variable"**
4. В поле "Variable Name" введите: `DATABASE_URL`
5. Вместо ввода значения вручную, нажмите на иконку **"{}"** (Reference) или выберите **"Reference"** из выпадающего списка
6. Выберите ваш **PostgreSQL сервис** из списка
7. Выберите переменную `DATABASE_URL` из списка переменных базы данных
8. Нажмите **"Add"** или **"Save"**

**Альтернативный способ (через баннер):**
- В разделе Variables вашего PostgreSQL сервиса есть баннер: "Trying to connect this database to a service? Add a Variable Reference"
- Нажмите на **"Variable Reference"** в этом баннере
- Выберите ваш **Web Service** из списка
- Railway автоматически добавит `DATABASE_URL` в ваш Web Service

**Проверка:**
- После добавления Variable Reference, в Variables вашего Web Service должна появиться переменная `DATABASE_URL`
- Значение должно быть серым и иметь иконку ссылки, что означает, что это ссылка на переменную из PostgreSQL сервиса

### Шаг 3: Проверка настройки

1. В панели проекта найдите ваш **PostgreSQL сервис**
2. Убедитесь, что он имеет статус **"Active"** (Активен) ✅
3. В разделе **Variables** вашего **Web Service** проверьте:
   - Есть ли переменная `DATABASE_URL`
   - Значение должно быть ссылкой на PostgreSQL (серый цвет, иконка ссылки)
   - НЕ должно быть пустым или отсутствовать

**Важно:** 
- `DATABASE_URL` в PostgreSQL сервисе - это переменная базы данных
- `DATABASE_URL` в Web Service - это ссылка на переменную базы данных (Variable Reference)
- Они должны быть связаны через Variable Reference!

### Шаг 4: Проверка подключения и деплоя

**Проверка подключения:**

1. После добавления Variable Reference, перезапустите ваш Web Service:
   - Откройте ваш Web Service
   - Перейдите в **"Deployments"**
   - Нажмите **"Redeploy"** или дождитесь автоматического деплоя

2. Проверьте логи приложения:
   - Откройте ваш Web Service → **"Deployments"** → выберите последний деплой → **"View Logs"**
   - Найдите строки:
     - `✅ Database ready` - база данных подключена
     - `📊 Текущее количество записей в courses_data: X` - показывает количество записей
     - `⚠️ DATABASE_URL не установлена` - база НЕ подключена (нужно добавить Variable Reference)
     - `⚠️ База данных недоступна` - проблема с подключением

**Если база пересоздается каждый раз:**
- Проверьте, что Variable Reference правильно настроен
- Убедитесь, что база данных не удаляется при перезапуске сервиса
- Проверьте, что используете `DATABASE_URL` (внутренний), а не `DATABASE_PUBLIC_URL` (внешний)

### Важные моменты

- **Variable Reference** - это способ связать переменные между сервисами в Railway
- **DATABASE_URL** (внутренний) - используется для подключения внутри Railway: `postgres.railway.internal:5432`
- **DATABASE_PUBLIC_URL** (внешний) - используется для внешних подключений: `interchange.proxy.rlwy.net:21093`
- **Для вашего приложения используйте DATABASE_URL** (внутренний)
- **Постоянная база данных** сохраняет данные между деплоями
- Railway по умолчанию создает постоянные базы данных
- Если вы используете бесплатный план Railway, база данных может иметь ограничения

### Частые проблемы

**Проблема:** "DATABASE_URL не установлена" в логах
- **Решение:** Добавьте Variable Reference в Web Service (см. Шаг 2)

**Проблема:** "База данных недоступна"
- **Решение:** Проверьте, что PostgreSQL сервис активен и используйте `DATABASE_URL`, а не `DATABASE_PUBLIC_URL`

**Проблема:** Данные теряются при деплое
- **Решение:** Убедитесь, что Variable Reference правильно настроен и база данных постоянная

### Альтернативное решение

Если проблема сохраняется, можно использовать внешнюю базу данных:
- **Supabase** (бесплатный план доступен)
- **Neon** (бесплатный план доступен)
- **Render PostgreSQL** (бесплатный план доступен)

Просто создайте базу данных в одном из этих сервисов и укажите `DATABASE_URL` в Railway.

